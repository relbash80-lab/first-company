# First Company — دليل النسخة التعليمية الأولى

> التاريخ: 2026-08-13
> الفرع المحلي: `codex/supabase-saas-foundation`
> الحالة: أساس SaaS حي على Supabase، ولم يُرفع إلى GitHub بعد.

## ماذا بُني؟

1. مصادقة المستخدمين بواسطة Supabase Auth.
2. إنشاء شركة جديدة للمستخدم بعد تسجيله لأول مرة.
3. عضوية وصلاحية مستقلة داخل كل شركة (`owner`, `manager`, `accountant`, `operator`, `viewer`).
4. خطط اشتراك وفترة تجريبية واستهلاك وفوترة قابلة للتوسعة.
5. سيارات وعملاء وحاويات وتكاليف ودفعات وسجل حالات ومستندات وتدقيق.
6. عزل بيانات الشركات عبر Row Level Security؛ المستخدم لا يقرأ بيانات شركة أخرى.
7. واجهة عربية RTL جديدة للدخول وإنشاء الحساب.
8. حفظ السيارة والعميل والتكاليف والحاوية داخل معاملة واحدة في PostgreSQL.

## كيف تتحرك البيانات؟

```text
إنشاء حساب
    ↓
Supabase Auth ينشئ user
    ↓
المستخدم ينشئ شركة
    ↓
create_organization ينشئ الشركة + عضوية owner + اشتراك trial
    ↓
كل طلب لاحق يحمل هوية المستخدم
    ↓
RLS يتحقق من عضويته قبل القراءة أو الكتابة
    ↓
save_vehicle_record يحفظ سجل السيارة المرتبط بالشركة كعملية ذرية
```

## لماذا دالة `save_vehicle_record`؟

إضافة سيارة ليست صفًا واحدًا فقط؛ قد تشمل عميلًا، رسوم شراء، شحنًا وحاوية. إذا فشل جزء واحد، فالمعاملة تلغي جميع الأجزاء. هذا يمنع وجود سيارة بلا رصيد صحيح أو حاوية مرتبطة جزئيًا.

## تشغيل النسخة محليًا

```powershell
npm install
Copy-Item .env.example .env.local
npm run dev -- --host 127.0.0.1 --no-open
```

ثم افتح:

`http://127.0.0.1:3000/first-company/`

يجب وضع رابط Supabase والمفتاح القابل للنشر فقط داخل `.env.local`. لا يوضع مفتاح `service_role` في الواجهة أبدًا.

## ما تم التحقق منه

- بناء Vite الإنتاجي نجح.
- صفحة الدخول وصفحة إنشاء الحساب عُرضتا بصريًا بالعربية.
- قاعدة Supabase الحية تحتوي على 19 جدولًا و47 سياسة RLS.
- جميع الجداول العامة مفعّل عليها RLS.
- دالة حفظ السيارة موجودة ومسموح للمستخدم المصادق بتنفيذها.
- Firebase أزيل من حزمة التطبيق ومن كود التشغيل.

## حدود هذا التسليم المرحلي

- لم يُنشأ حساب مستخدم وهمي، ولم يتم اختبار الرحلة بعد البريد المؤكد بهوية حقيقية.
- الدفع الإلكتروني للاشتراكات وWebhook لم يُفعّلا بعد.
- لم يحدث نشر أو Push إلى GitHub؛ العمل ما زال على الفرع المحلي.

## الخطوة التالية المقترحة

إنشاء حساب المالك الحقيقي من شاشة التطبيق، تأكيد البريد، إنشاء الشركة، ثم إدخال سيارة اختبار واحدة. بعدها نختبر العزل بإنشاء شركة ثانية ونثبت أن بيانات كل شركة لا تظهر للأخرى.
